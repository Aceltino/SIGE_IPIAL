$(document).ready(function() {
    $('#numeroinput').on('input', function() {
      var numero = $(this).val();
      var regex = /^[0-9]+$/;
  
      if (!regex.test(numero)) {
        $(this).val('');
      }
    });
  });

  $(document).ready(function() {
    $('#numeroinput1').on('input', function() {
      var numero = $(this).val();
      var regex = /^[0-9]+$/;
  
      if (!regex.test(numero)) {
        $(this).val('');
      }
    });
  });

  $(document).ready(function() {
    $('#numeroinput2').on('input', function() {
      var numero = $(this).val();
      var regex = /^[0-9]+$/;
  
      if (!regex.test(numero)) {
        $(this).val('');
      }
    });
  });

  $(document).ready(function() {
    $('#numeroinput3').on('input', function() {
      var numero = $(this).val();
      var regex = /^[0-9]+$/;
  
      if (!regex.test(numero)) {
        $(this).val('');
      }
    });
  });

  $(document).ready(function() {
    $('#numeroinput4').on('input', function() {
      var numero = $(this).val();
      var regex = /^[0-9]+$/;
  
      if (!regex.test(numero)) {
        $(this).val('');
      }
    });
  });

  $(document).ready(function() {
    $('#numeroinput5').on('input', function() {
      var numero = $(this).val();
      var regex = /^[0-9]+$/;
  
      if (!regex.test(numero)) {
        $(this).val('');
      }
    });
  });

  $(document).ready(function() {
    $('#numeroinput6').on('input', function() {
      var numero = $(this).val();
      var regex = /^[0-9]+$/;
  
      if (!regex.test(numero)) {
        $(this).val('');
      }
    });
  });

  $(document).ready(function() {
    $('#numeroinput7').on('input', function() {
      var numero = $(this).val();
      var regex = /^[0-9]+$/;
  
      if (!regex.test(numero)) {
        $(this).val('');
      }
    });
  });

  $(document).ready(function() {
    $('#numeroinput8').on('input', function() {
      var numero = $(this).val();
      var regex = /^[0-9]+$/;
  
      if (!regex.test(numero)) {
        $(this).val('');
      }
    });
  });

  $(document).ready(function() {
    $('#numeroinput9').on('input', function() {
      var numero = $(this).val();
      var regex = /^[0-9]+$/;
  
      if (!regex.test(numero)) {
        $(this).val('');
      }
    });
  });